#ifndef CONFIG_H
#define CONFIG_H

/* the data file directory, as set by meson */
static const char *CCQ_DATA_DIR = "@CCQ_DATA_DIR@";

/* database to query if no -d flag passed */
static const char *DEFAULT_DB = "db";
static const char *DEFAULT_SL = "zh";

/* the keypresses that ccq accepts as "PASS" */
static const char PASS_CHARS[] = {
	'\n',
	'p',
	'P',
	' '
};

/* 'n'ewest first, 'o'ldest first, 'r'andom */
static const char DEFAULT_ORDER = 'r';

#endif // CONFIG_H
